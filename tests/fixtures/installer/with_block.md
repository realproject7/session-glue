# Global Agent Instructions

Some pre-existing user rules live here.

<!-- BEGIN SESSION GLUE (managed) -->
## Session Glue

(Managed content lives between these markers; a future updater replaces it
idempotently. This fixture is only used to test block detection.)
<!-- END SESSION GLUE (managed) -->

More user rules after the managed block.
